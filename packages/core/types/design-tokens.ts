import type {
  Colors,
  Typography,
  Spacing,
  BorderRadius,
  Shadows,
  Transitions,
  ZIndex,
  Breakpoints
} from '@healthcare-chat/design-tokens'

export interface DesignTokens {
  colors: Colors
  typography: Typography
  spacing: Spacing
  borderRadius: BorderRadius
  shadows: Shadows
  transitions: Transitions
  zIndex: ZIndex
  breakpoints: Breakpoints
}

export type { Colors, Typography, Spacing, BorderRadius, Shadows, Transitions, ZIndex, Breakpoints } 